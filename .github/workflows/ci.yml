# CI Pipeline - manti_finance_dev
# I job reali (lint, test, build) saranno attivati dopo issue #2 (Setup React+TypeScript)
# Per ora il workflow e' in modalita' ready - non blocca le notifiche

name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  # --------------------------------------------------
  # JOB PLACEHOLDER - passa sempre (exit 0)
  # Rimuovere e decommentare i job reali dopo issue #2
  # --------------------------------------------------
  ci-ready:
    name: CI Ready (placeholder - attesa issue #2)
    runs-on: ubuntu-latest
    steps:
      - name: Stato pipeline
        run: |
          echo "=================================================="
          echo "CI skeleton attivo - manti_finance_dev"
          echo "I job reali saranno abilitati dopo issue #2"
          echo "Stack: React 18 + TypeScript + Vite + Vitest"
          echo "=================================================="

  # --------------------------------------------------
  # JOB 1: Type check + Lint
  # Da attivare dopo issue #2 (Setup React+TypeScript)
  # --------------------------------------------------
  # lint:
  #   name: TypeScript & ESLint
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v4
  #     - uses: actions/setup-node@v4
  #       with:
  #         node-version: '20'
  #         cache: 'npm'
  #     - run: npm ci
  #     - run: npm run type-check  # tsc --noEmit
  #     - run: npm run lint        # eslint src/

  # --------------------------------------------------
  # JOB 2: Unit Tests
  # Da attivare dopo issue #2 e i primi servizi con test
  # --------------------------------------------------
  # test:
  #   name: Vitest Unit Tests
  #   runs-on: ubuntu-latest
  #   needs: lint
  #   steps:
  #     - uses: actions/checkout@v4
  #     - uses: actions/setup-node@v4
  #       with:
  #         node-version: '20'
  #         cache: 'npm'
  #     - run: npm ci
  #     - run: npm run test  # vitest run

  # --------------------------------------------------
  # JOB 3: Build check
  # Da attivare dopo issue #2
  # --------------------------------------------------
  # build:
  #   name: Vite Build
  #   runs-on: ubuntu-latest
  #   needs: test
  #   steps:
  #     - uses: actions/checkout@v4
  #     - uses: actions/setup-node@v4
  #       with:
  #         node-version: '20'
  #         cache: 'npm'
  #     - run: npm ci
  #     - run: npm run build

# CI Pipeline — manti_finance_dev
# Questo workflow viene completato nella issue #20 (GitHub Actions)
# quando il codice applicativo sara' presente.
# Per ora e' uno skeleton commentato pronto per essere attivato.

name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  # ---------------------------------------------------------
  # JOB 1: Type check + Lint
  # Da attivare dopo issue #2 (Setup React+TypeScript)
  # ---------------------------------------------------------
  # lint:
  #   name: TypeScript & ESLint
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v4
  #     - uses: actions/setup-node@v4
  #       with:
  #         node-version: '20'
  #         cache: 'npm'
  #     - run: npm ci
  #     - run: npm run type-check   # tsc --noEmit
  #     - run: npm run lint         # eslint src/

  # ---------------------------------------------------------
  # JOB 2: Unit Tests
  # Da attivare dopo issue #2 e i primi servizi con test
  # ---------------------------------------------------------
  # test:
  #   name: Vitest Unit Tests
  #   runs-on: ubuntu-latest
  #   needs: lint
  #   steps:
  #     - uses: actions/checkout@v4
  #     - uses: actions/setup-node@v4
  #       with:
  #         node-version: '20'
  #         cache: 'npm'
  #     - run: npm ci
  #     - run: npm run test         # vitest run

  # ---------------------------------------------------------
  # JOB 3: Build check
  # Da attivare dopo issue #2
  # ---------------------------------------------------------
  # build:
  #   name: Vite Build
  #   runs-on: ubuntu-latest
  #   needs: test
  #   steps:
  #     - uses: actions/checkout@v4
  #     - uses: actions/setup-node@v4
  #       with:
  #         node-version: '20'
  #         cache: 'npm'
  #     - run: npm ci
  #     - run: npm run build

  # Placeholder per mantenere il workflow valido
  # fino all'attivazione dei job reali
  placeholder:
    name: Skeleton placeholder (remove after issue #2)
    runs-on: ubuntu-latest
    steps:
      - run: echo "CI skeleton attivo. Decommentare i job dopo issue #2."
